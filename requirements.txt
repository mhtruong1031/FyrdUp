uagents==0.11.0
opencv-python>=4.8.0
numpy>=1.24.0
google-generativeai>=0.8.0
google-adk>=1.0.0
rich>=13.7.0
cv-bridge
foxglove-sdk
nest_asyncio
pyyaml>=6.0
transformers>=4.35.0
torch>=2.1.0
Pillow>=10.0.0
# NOTE: google-adk needs pydantic>=2, uagents pins pydantic<2.
# Install order: pip install uagents==0.11.0 && pip install google-adk pydantic>=2 --force-reinstall --no-deps-check
# uagents works with pydantic v2 for simple Model subclasses.
